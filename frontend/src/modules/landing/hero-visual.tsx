"use client";

import { HeroGraphic } from "./hero-graphic";

/**
 * The landing hero visual: an illustrated, animated night station.
 * See `hero-graphic.tsx` for the metaphor and the motion.
 */
export function HeroVisual() {
	return <HeroGraphic />;
}
